import * as labels from './lib.enums.labels';
import * as message from './lib.enums.message';
import * as query from './lib.enums.query';
import * as status from './lib.enums.status';

export default {
  ...labels,
  ...message,
  ...query,
  ...status,
};
